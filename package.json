{
  "name": "jessbirthday",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "deploy": "npx wrangler pages deploy ./build --project-name jessbirthday"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^2.0.0",
    "@sveltejs/kit": "^2.17.3",
    "svelte": "^5.20.5",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@sveltejs/adapter-cloudflare": "^2.0.0"
  }
}
